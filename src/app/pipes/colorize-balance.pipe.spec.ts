import { ColorizeBalancePipe } from './colorize-balance.pipe';

describe('ColorizeBalancePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorizeBalancePipe();
    expect(pipe).toBeTruthy();
  });
});
